import styled from "styled-components";

const TooltipContainer = styled.div`
    display: flex;
`;

const Label = styled.span `
    margin-right: 0.5rem;
`;

const InformationText = styled.b `
    
`;

export {
    TooltipContainer,
    Label,
    InformationText
}